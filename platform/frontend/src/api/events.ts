/** API-ready event endpoints — swap mock service when backend is live */
export {
  fetchEvent,
  saveEvent,
  duplicateEvent,
  deleteEvent,
  fetchAnalytics,
  checkSlugAvailable,
} from '@/services/eventService'
